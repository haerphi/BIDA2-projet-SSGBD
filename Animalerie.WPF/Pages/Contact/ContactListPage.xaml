<Page x:Class="Animalerie.WPF.Pages.ContactListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:Animalerie.WPF.ViewModels.Contacts"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=vm:ContactListViewModel}"
      Title="Gestion des Contacts">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gestion des Contacts" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="#F4F4F4" Padding="10" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <Label Content="Nom :" Padding="0,0,0,2"/>
                    <TextBox Width="150" Text="{Binding FilterLastname, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <Label Content="Prénom :" Padding="0,0,0,2"/>
                    <TextBox Width="150" Text="{Binding FilterFirstname, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <Button Content="Réinitialiser" 
                        Command="{Binding ResetFiltersCommand}" 
                        VerticalAlignment="Bottom" 
                        Padding="10,5" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Contacts}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
                <DataGridTextColumn Header="GSM" Binding="{Binding Gsm}" Width="120"/>
                <DataGridTextColumn Header="Localité" Binding="{Binding Localite}" Width="120"/>

                <DataGridTemplateColumn Header="Actions" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Détails" 
                                    Command="{Binding DataContext.ShowDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
