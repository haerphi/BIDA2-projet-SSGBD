<Page x:Class="Animalerie.WPF.Pages.AnimalAddPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:Animalerie.WPF.ViewModels"
      mc:Ignorable="d"
      Title="Ajouter un Animal"
      d:DataContext="{d:DesignInstance Type=vm:AnimalAddViewModel}">

    <ScrollViewer Margin="20">
        <StackPanel>
            <TextBlock Text="Ajouter un nouvel animal" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label Content="Identifiant :" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="5"/>

                <Label Content="Nom :" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Nom}" Grid.Row="1" Grid.Column="1" Margin="5"/>

                <Label Content="Type :" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding TypesList}" SelectedItem="{Binding SelectedType}" Grid.Row="2" Grid.Column="1" Margin="5"/>

                <Label Content="Sexe :" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding SexesList}" SelectedItem="{Binding SelectedSexe}" Grid.Row="3" Grid.Column="1" Margin="5"/>

                <Label Content="Date Naissance :" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding DateNaissance}" Grid.Row="4" Grid.Column="1" Margin="5"/>

                <Label Content="Couleurs :" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CouleursInput}" Grid.Row="5" Grid.Column="1" Margin="5"/>

                <Label Content="Raison Entrée :" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding RaisonsList}" SelectedItem="{Binding SelectedRaison}" Grid.Row="6" Grid.Column="1" Margin="5"/>

                <Label Content="Contact :" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding ContactsList}" SelectedItem="{Binding SelectedContact}" Grid.Row="7" Grid.Column="1" Margin="5" DisplayMemberPath="Nom"/>
            </Grid>

            <Button Content="Enregistrer l'animal" 
                    Command="{Binding SaveCommand}"
                    Margin="0,20" Height="40" Background="#27AE60" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </ScrollViewer>
</Page>